import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  activityContainer: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center'
  },
  activityIndicator: {
    height: 80,
    width: 80
  }
});
